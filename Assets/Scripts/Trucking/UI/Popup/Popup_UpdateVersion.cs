using UnityEngine;

namespace Trucking.UI.Popup
{
    public class Popup_UpdateVersion : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
    
}
